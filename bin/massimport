#!/usr/bin/env node

var mass_importer = require('../libs/mass_importer');

console.log("Starting import");

mass_importer.import(function(err) {
  if(err) {
    throw err;
  }
  
  console.log("Done");
});